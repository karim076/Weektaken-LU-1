<!DOCTYPE html>
<html lang="nl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
</head>
<body class="bg-light">
    <%- include('../partials/navbar') %>
    
    <div class="container mt-4">
        <!-- Breadcrumb -->
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="/dashboard">Dashboard</a></li>
                <li class="breadcrumb-item"><a href="/customer/rentals">Mijn Verhuur</a></li>
                <li class="breadcrumb-item active">Verhuur #<%= rental.rental_id %></li>
            </ol>
        </nav>

        <div class="row">
            <div class="col-lg-8">
                <!-- Film & Rental Info Card -->
                <div class="card shadow-sm mb-4">
                    <div class="card-header bg-white">
                        <div class="d-flex justify-content-between align-items-center">
                            <h5 class="mb-0 text-primary">
                                <i class="bi bi-film"></i>
                                <%= rental.film_title %>
                            </h5>
                            <span class="badge bg-primary">
                                #<%= rental.rental_id %>
                            </span>
                        </div>
                    </div>
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-6">
                                <h6 class="text-muted mb-3">
                                    <i class="bi bi-info-circle"></i> Film Informatie
                                </h6>
                                <p><strong>Titel:</strong> <%= rental.film_title %></p>
                                <p><strong>Categorie:</strong> 
                                    <span class="badge bg-secondary"><%= rental.category %></span>
                                </p>
                                <% if (rental.description) { %>
                                <p><strong>Beschrijving:</strong></p>
                                <p class="text-muted small"><%= rental.description %></p>
                                <% } %>
                            </div>
                            <div class="col-md-6">
                                <h6 class="text-muted mb-3">
                                    <i class="bi bi-calendar-check"></i> Verhuur Details
                                </h6>
                                <p><strong>Huur Datum:</strong> <%= new Date(rental.rental_date).toLocaleDateString('nl-NL') %></p>
                                <p><strong>Verwachte Terug:</strong> 
                                    <% if (rental.expected_return_date) { %>
                                        <span class="text-info">
                                            <%= new Date(rental.expected_return_date).toLocaleDateString('nl-NL') %>
                                        </span>
                                    <% } else { %>
                                        <span class="text-muted">N/A</span>
                                    <% } %>
                                </p>
                                <% if (rental.return_date) { %>
                                <p><strong>Teruggegeven:</strong> 
                                    <span class="text-success">
                                        <%= new Date(rental.return_date).toLocaleDateString('nl-NL') %>
                                    </span>
                                </p>
                                <% } %>
                                <p><strong>Bedrag:</strong> 
                                    <span class="h5 text-success">€<%= parseFloat(rental.amount || 0).toFixed(2) %></span>
                                </p>
                                <p><strong>Huidige Status:</strong>
                                    <% if (rental.status === 'pending') { %>
                                        <span class="badge bg-warning text-dark">In Behandeling</span>
                                    <% } else if (rental.status === 'paid') { %>
                                        <span class="badge bg-success">Betaald</span>
                                    <% } else if (rental.status === 'rented') { %>
                                        <span class="badge bg-primary">Gehuurd</span>
                                    <% } else if (rental.status === 'returned') { %>
                                        <span class="badge bg-info">Teruggebracht</span>
                                    <% } %>
                                </p>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Status Timeline -->
                <div class="card shadow-sm">
                    <div class="card-header bg-white">
                        <h5 class="mb-0 text-primary">
                            <i class="bi bi-clock-history"></i>
                            Verhuur Status
                        </h5>
                    </div>
                    <div class="card-body">
                        <div class="timeline">
                            <!-- Step 1: Aanvraag -->
                            <div class="timeline-item completed">
                                <div class="timeline-marker bg-success text-white">
                                    <i class="bi bi-check"></i>
                                </div>
                                <div class="timeline-content">
                                    <h6 class="text-success">Aanvraag Ingediend</h6>
                                    <p class="text-muted small mb-1">
                                        <%= new Date(rental.rental_date).toLocaleString('nl-NL') %>
                                    </p>
                                    <p class="small text-muted">Film verhuur aanvraag is succesvol ingediend.</p>
                                </div>
                            </div>

                            <!-- Step 2: Betaling -->
                            <div class="timeline-item <%= ['paid', 'rented', 'returned'].includes(rental.status) ? 'completed' : rental.status === 'paid' ? 'active' : 'pending' %>">
                                <div class="timeline-marker <%= ['paid', 'rented', 'returned'].includes(rental.status) ? 'bg-success text-white' : rental.status === 'paid' ? 'bg-primary text-white' : 'bg-light text-muted' %>">
                                    <i class="bi <%= ['paid', 'rented', 'returned'].includes(rental.status) ? 'bi-check' : 'bi-credit-card' %>"></i>
                                </div>
                                <div class="timeline-content">
                                    <h6 class="<%= ['paid', 'rented', 'returned'].includes(rental.status) ? 'text-success' : rental.status === 'paid' ? 'text-primary' : 'text-muted' %>">Betaling</h6>
                                    <p class="small <%= ['paid', 'rented', 'returned'].includes(rental.status) ? 'text-success' : 'text-muted' %>">
                                        <%= ['paid', 'rented', 'returned'].includes(rental.status) ? 'Betaling is verwerkt en bevestigd.' : 'Wacht op betaling bevestiging.' %>
                                    </p>
                                </div>
                            </div>

                            <!-- Step 3: Ophalen -->
                            <div class="timeline-item <%= ['rented', 'returned'].includes(rental.status) ? 'completed' : rental.status === 'rented' ? 'active' : 'pending' %>">
                                <div class="timeline-marker <%= ['rented', 'returned'].includes(rental.status) ? 'bg-success text-white' : rental.status === 'rented' ? 'bg-primary text-white' : 'bg-light text-muted' %>">
                                    <i class="bi <%= ['rented', 'returned'].includes(rental.status) ? 'bi-check' : 'bi-box-arrow-down' %>"></i>
                                </div>
                                <div class="timeline-content">
                                    <h6 class="<%= ['rented', 'returned'].includes(rental.status) ? 'text-success' : rental.status === 'rented' ? 'text-primary' : 'text-muted' %>">Film Ophalen</h6>
                                    <p class="small <%= ['rented', 'returned'].includes(rental.status) ? 'text-success' : 'text-muted' %>">
                                        <%= ['rented', 'returned'].includes(rental.status) ? 'Film is opgehaald en in je bezit.' : 'Film klaar om opgehaald te worden.' %>
                                    </p>
                                </div>
                            </div>

                            <!-- Step 4: Teruggeven -->
                            <div class="timeline-item <%= rental.status === 'returned' ? 'completed' : 'pending' %>">
                                <div class="timeline-marker <%= rental.status === 'returned' ? 'bg-success text-white' : 'bg-light text-muted' %>">
                                    <i class="bi <%= rental.status === 'returned' ? 'bi-check' : 'bi-box-arrow-up' %>"></i>
                                </div>
                                <div class="timeline-content">
                                    <h6 class="<%= rental.status === 'returned' ? 'text-success' : 'text-muted' %>">Film Terugbrengen</h6>
                                    <% if (rental.return_date) { %>
                                        <p class="text-muted small mb-1">
                                            <%= new Date(rental.return_date).toLocaleString('nl-NL') %>
                                        </p>
                                        <p class="small text-success">Film is succesvol teruggebracht.</p>
                                    <% } else { %>
                                        <p class="small text-muted">Film moet worden teruggebracht.</p>
                                    <% } %>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Sidebar -->
            <div class="col-lg-4">
                <!-- Quick Actions -->
                <div class="card shadow-sm mb-4">
                    <div class="card-header bg-light">
                        <h6 class="mb-0">
                            <i class="bi bi-lightning-charge text-warning"></i>
                            Acties
                        </h6>
                    </div>
                    <div class="card-body">
                        <div class="d-grid gap-2">
                            <a href="/films/<%= rental.film_id %>" class="btn btn-outline-primary">
                                <i class="bi bi-info-circle"></i> Film Details
                            </a>
                            <a href="/customer/rentals" class="btn btn-outline-secondary">
                                <i class="bi bi-arrow-left"></i> Terug naar Overzicht
                            </a>
                            <% if (rental.status === 'pending') { %>
                            <button class="btn btn-success" disabled>
                                <i class="bi bi-credit-card"></i> Wacht op Betaling...
                            </button>
                            <% } %>
                        </div>
                    </div>
                </div>

                <!-- Rental Summary -->
                <div class="card shadow-sm">
                    <div class="card-header bg-light">
                        <h6 class="mb-0">
                            <i class="bi bi-receipt text-info"></i>
                            Verhuur Samenvatting
                        </h6>
                    </div>
                    <div class="card-body">
                        <div class="d-flex justify-content-between mb-2">
                            <span>Film:</span>
                            <strong><%= rental.film_title %></strong>
                        </div>
                        <div class="d-flex justify-content-between mb-2">
                            <span>Verhuur ID:</span>
                            <span class="text-muted">#<%= rental.rental_id %></span>
                        </div>
                        <div class="d-flex justify-content-between mb-2">
                            <span>Status:</span>
                            <% if (rental.status === 'pending') { %>
                                <span class="badge bg-warning text-dark">In Behandeling</span>
                            <% } else if (rental.status === 'paid') { %>
                                <span class="badge bg-success">Betaald</span>
                            <% } else if (rental.status === 'rented') { %>
                                <span class="badge bg-primary">Gehuurd</span>
                            <% } else if (rental.status === 'returned') { %>
                                <span class="badge bg-info">Teruggebracht</span>
                            <% } %>
                        </div>
                        <hr>
                        <div class="d-flex justify-content-between">
                            <strong>Totaal Bedrag:</strong>
                            <strong class="text-success">€<%= parseFloat(rental.amount || 0).toFixed(2) %></strong>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <%- include('../partials/footer') %>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
    <style>
        .timeline {
            position: relative;
            padding-left: 30px;
        }
        
        .timeline:before {
            content: '';
            position: absolute;
            left: 15px;
            top: 0;
            bottom: 0;
            width: 2px;
            background: #dee2e6;
        }
        
        .timeline-item {
            position: relative;
            margin-bottom: 2rem;
            padding-left: 30px;
        }
        
        .timeline-marker {
            position: absolute;
            left: -38px;
            width: 30px;
            height: 30px;
            border-radius: 50%;
            background: #6c757d;
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 12px;
            border: 2px solid #fff;
            box-shadow: 0 0 0 3px #dee2e6;
        }
        
        .timeline-item.active .timeline-marker {
            background: #0d6efd;
            box-shadow: 0 0 0 3px #b3d7ff;
        }
        
        .timeline-item.completed .timeline-marker {
            background: #198754;
            box-shadow: 0 0 0 3px #b3e5c7;
        }
        
        .timeline-item.pending .timeline-marker {
            background: #6c757d;
            color: #adb5bd;
        }
    </style>
</body>
</html>
